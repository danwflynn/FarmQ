version: "3.9"

services:
  farmq:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: farmq
    ports:
      - "8080:8080"
    environment:
      - LOG_LEVEL=debug
    restart: unless-stopped
    command: ["./farmq"]
